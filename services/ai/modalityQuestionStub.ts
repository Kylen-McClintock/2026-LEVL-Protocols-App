export async function getModalityRecommendationReason(modalityId: string, userId: string): Promise<string> {
  console.log('Stub: Getting modality recommendation reason for', modalityId)
  return "This modality was ranked highly for you based on its overall longevity benefit and low friction."
}
